<div class="container mt-4">
    <h1>List all jobs</h1>
        <%@jobs.each do |j|%>
            <%if j.approved_by != 0%>
                <li><%= link_to j.title, job_path(j) %></li>
            <%end%>
        <%end%>
    

    <%= link_to "Create a new Job", new_job_path %>
</div>