<div class="container">
    <h1>Create a new Sector</h1>
    <%if @sector.errors.any?%>
    <h4 class="alert-heading">The following errors prevented the sector from being saved</h4>
        <ul>
            <% @sector.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
    <%end%>
    <%= form_with(model: @sector) do |f|%>
        <div>
            <%= f.label :name %>
            <%= f.text_field :name %> 
        </div>
        <br>
            <%=f.submit "Submit"%>
    <%end%>
</div>